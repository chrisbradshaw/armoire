<% @outfits.each do |outfit|  %>

  <%= outfit.name || "Your Outfit" %>
  <%= button_to "Approve",status_outfit_path(outfit), method: :put, params: {status: 'approve'} %>
  <%= button_to "Reject", status_outfit_path(outfit), method: :put, params: {status: 'reject'} %>

<% end %>
