<% provide(:title, "Log in") %>
<h1>Log in</h1>

<div class ="row">
  <div class="col-md-6 col-md-offset-3"> 
    <%= form_for(:session, url: new_user_session_path) do |f|%>
      <%= f.label :email %>
      <%= f.email_field :email, class: 'form-control' %>

      <%= f.label :password %>
      <%= f.password_field :password, class: 'form-control' %>

      <%=f.submit "Log in", class: "btn btn-primary" %>
    <% end %>

    <p>With ARMOIRE, get dressed, not stressed. <%=link_to "Sign up now!", new_user_registration_path %></p>
    </div>
  </div>


  <!--     
 <%= select_tag "user[id]", options_from_collection_for_select(User.all, :id, :name) %>
 <%= submit_tag "Sign In"%> -->