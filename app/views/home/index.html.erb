
<div class="container" style="background-image: url('<%= asset_path("fashion/bg-header.jpg")%>')">
    <h1 class="center bold-header" id="armoire">Armoire</h1>
    <h2 class="center italic-h2">Style your life</h2>

      <div class="row">

        <div class="col-md-4">
          <p>Organized Wardrobe</p>
             <%= link_to image_tag("girl-pose.svg", alt: "girl pose"),
                '#' %>
        </div><!-- col-md-4 -->

         <div class="widget">
         
            <div class="date">
                <% t =Time.now %>
              <span><%= t.strftime( "%A" ) %></span>
              <span id="month"><%= t.strftime("%B") %></span>
              <span id="month"><%= t.strftime("%d") %></span>
            </div>
          <div>
        <span><%= @summary %></span>
        <span><%= @temperature %></span>

    <!-- 
      need to insert weather dynamically!
      yahoo API? 
     -->
  </div>
</div>


      <% if !signed_in? %>
          <%= link_to "Sign up now!", new_user_registration_path, class: "btn btn-lg-secondary" %>
          <% else %>
           <%= link_to "Start building your wardrobe!", new_shoe_path, class: "btn btn-md btn-primary" %>
      <% end %>
          </div>

      </div><!-- row -->

  </div><!-- center jumbotron -->
</div><!-- container -->